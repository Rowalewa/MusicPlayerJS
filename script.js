// for simple arrow functions returning one and just one operation, no need to wrap them in curly brackets and return
// i.e const function = (num1, num2) => num1 + num2;
// instead of const function = (num1, num2) => {return num1 + num2};